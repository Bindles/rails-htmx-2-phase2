<%= form_with(model: wheelcolor, local: false) do |form| %>
  <% if wheelcolor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wheelcolor.errors.count, "error") %> prohibited this wheelcolor from being saved:</h2>

      <ul>
        <% wheelcolor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
